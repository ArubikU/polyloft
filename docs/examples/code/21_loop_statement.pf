// Test: Loop statement (infinite loop with break)
println("=== Loop Statement Test ===")

let count = 0
loop
    println("Count: " + count.toString())
    count = count + 1
    if count >= 3:
        break
    end
end

println("✓ Loop statement test passed")
